<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thgmobi.testeshopper">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_shopper"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_shopper_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity"
            android:screenOrientation="locked">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:screenOrientation="portrait"
            android:name=".activities.RegisterActivity"
            android:label="Formulário de cadastro"/>
        <activity
            android:screenOrientation="portrait"
            android:name=".activities.HomeActivity"
            android:label="Lista de Produtos"/>
        <activity
            android:screenOrientation="portrait"
            android:name=".activities.ProductoActivity"
            android:label="Formulário de produto"/>
    </application>

</manifest>